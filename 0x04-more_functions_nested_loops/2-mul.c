#include "main.h"
   /**
     *mul - Mulitiples two integers
     *@x: The first number to multiply
     *@y: The second number to multiply with first number
     *Return: The result of multication between x param and y param
     */

int mul(int x, int y)
{
	return (x * y);
}
